# Night Shift — 2026-09-09

Sesión autónoma multifase. Rama `night-shift/2026-09-09`, base `c6c29d9` (main, confirmado limpio y coincidente con `origin/main`).

## Fase A — Auditoría de baseline

- 00:00 HEAD real confirmado: `c6c29d9`, working tree limpio, `origin/main` sin divergencia. Rama creada.
- Auditoría exhaustiva de infraestructura de persistencia: sin `wrangler.toml`/`.jsonc` en el repo, sin `wrangler`/`@cloudflare/workers-types` en `package.json`/`node_modules`, `.wrangler/` local solo contiene caché de Miniflare/observability (SQLite), `.env.example` no documenta ninguna variable de storage. Conclusión confirmada: **no existe D1/KV/Durable Objects/Supabase previo** — cualquier persistencia hay que diseñarla desde cero, honestamente marcada como pendiente de aprovisionamiento humano.

## Fase B — Diseño de arquitectura Rank/LP persistence

- Modelo `RankSnapshot` (dominio puro, `src/lib/rank-history/types.ts`): `puuid`, `queueType` (`RANKED_SOLO_5x5`), `tier`/`rank`/`leaguePoints` opcionales (honesto para no clasificados), `wins`/`losses`, `observedAt`, `source` (`overview`|`cron`).
- Orden de rango real tier→division→LP en `rank-order.ts` (`compareRank`), separado deliberadamente de `compareTierDivision` (tier→division, sin LP) usado solo para detectar transiciones reales — una fluctuación de LP dentro de la misma división nunca se reporta como cambio de rango.
- Política de deduplicación (`dedupe.ts`): inserta solo si es la primera observación, si tier/rank/LP/wins/losses cambiaron, o si pasaron ≥6h (`HEARTBEAT_MS`) desde el último snapshot — evita miles de filas casi idénticas sin perder prueba de seguimiento activo.
- Patrón repositorio/adaptador: `types.ts` (dominio) → `d1-repository.ts` (el único fichero que conoce nombres de tabla/columnas D1) → endpoints HTTP (orquestación). Sin SQL fuera del adaptador, sin lógica Riot duplicada.
- Migración SQL (`migrations/0001_rank_snapshots.sql`) con cabecera documentando el proceso humano de 3 pasos (crear D1 → bind `DB` → migrar `--remote`) — **no se ejecuta sola**, requiere aprovisionamiento manual explícito (bloqueador de infraestructura, reportado honestamente).
- Endpoint de lectura pública `GET /api/riot/rank-history` (solo lectura, se degrada a `available:false` sin storage/PUUID/error — nunca un 500) y endpoint de escritura `POST /api/riot/rank-snapshot-cron` (gateado por `RANK_SNAPSHOT_CRON_SECRET` vía `Authorization: Bearer`, nunca acepta PUUID del cliente).
- Escritura desde `/api/riot/overview` vía `waitUntil()` (cero latencia añadida a la respuesta pública), reutilizando `resolveSelfAccountPuuid` (envoltorio nuevo sobre la cuenta ya cacheada 24h) — en el caso normal, 0 llamadas Riot nuevas para la observación.

## Fase C — Implementación V1

- Módulo completo `src/lib/rank-history/` (types, d1-types, rank-order, dedupe, observe, evolution, d1-repository, index) + wiring en `functions/api/riot/overview.ts` y `src/lib/riot/index.ts`.
- 54 tests nuevos del dominio (`test/lib/rank-history/*.test.ts`) + 11 tests de los dos endpoints HTTP (`test/api/rank-history.test.ts`, `test/api/rank-snapshot-cron.test.ts`) — 65 tests nuevos en total, todos verdes.
- Dos bugs autodetectados y corregidos durante la implementación (nunca reportados por el usuario): fuga de reloj real en `recordObservationIfDue` (fixeado con parámetro `now` inyectable) y uso inicial incorrecto de `compareRank` (con LP) en la detección de transiciones — corregido a `compareTierDivision` antes de ejecutar ningún test.
- Validación completa tras la implementación: 819/819 tests del proyecto en verde, `npx tsc --noEmit` limpio, `npx eslint .` limpio (incluyendo un patrón de alias `this`→`self` en los mocks `FakeD1` de test que eslint rechazaba — reescrito a métodos arrow-function), `npm run build` completa sin errores (187 páginas).
- Commit `032f17e`: "night(rank-history): arquitectura y V1 de persistencia de Rank/LP".

## Fase D — UI de evolución de rango

- Resuelto §27 ("Chart LP" honesto cruzando tiers): un LP crudo en el eje Y mentiría (Diamond I 80 LP > Master 0 LP numéricamente, pero es un rango inferior). Solución: `rankOrdinal()` (nuevo, en `rank-order.ts`) aplana tier→division→LP a un único número monótono usado SOLO para la posición Y del sparkline — nunca se imprime en ningún sitio; todo el texto que ve el usuario sale de tier/rank/LP reales vía `formatRankLabel()` (nuevo `format.ts`).
- `chart.ts` (nuevo): `buildRankChartPoints` proyecta el histórico a coordenadas SVG con espaciado por ÍNDICE (no por tiempo real — con heartbeat de 6h los huecos son irregulares y un eje temporal aplastaría los puntos); `buildSparklinePath` genera el `d` de un `<path>` a mano, sin librería de charting (mismo principio que el Timeline).
- `render.ts` (nuevo): `buildRankEvolutionView()` — toda la lógica de "qué mostrar" en una función pura y testeable (component solo pinta). Cuatro estados: `hidden` (storage no configurado — el bloque desaparece sin más, nunca un error), `empty` (0 snapshots reales), `single` (1 snapshot: solo "Seguimiento desde..." + rango actual, sin chart/peak), `chart` (2+: sparkline + "Peak observado: X" + "Seguimiento desde..." + última transición real si la hay). Copy exacto verificado por test: nunca "Peak Season", nunca una transición inventada sin base real.
- Nuevo componente `src/components/live/RankEvolution.astro`, integrado dentro de la sección existente "01 / Ahora" (justo debajo del season-record), no como página ni dashboard aparte. Fetch propio a `/api/riot/rank-history` (mismo patrón que `PerformanceProfile.astro`: cada bloque pide solo lo que necesita, nunca bloquea el resto). Hook de debug `__rankEvolutionDebugRender` solo en `import.meta.env.DEV`, confirmado ausente de `dist/` tras el build.
- QA visual en navegador (dev server + hook de debug, sin tocar D1 real) en los 4 estados × mobile (375)/tablet (768)/desktop: todos legibles, chart escala correctamente, estado `hidden` confirmado con `offsetHeight: 0` (sin hueco visual).
- Bug de tipado detectado por `astro check` (no por `tsc --noEmit` plano): `hidden` no es un atributo válido en `SVGAttributes` de Astro — se resolvió envolviendo el `<svg>` en un `<div>` que sí lleva `hidden` (mismo patrón que `MatchTimelinePanel.astro`).
- Nuevos módulos también testeados: `format.test.ts`, `chart.test.ts`, `render.test.ts` (dominio `rank-history`) + `rankOrdinal` añadido a `rank-order.test.ts` + `madridDate` (nuevo helper en `lib/time.ts`) en `test/lib/time.test.ts` — 26 tests nuevos.
- Validación: 845/845 tests en verde, `npx tsc --noEmit` limpio, `npx eslint .` limpio, `npm run build` (`astro check && astro build`) sin errores ni warnings nuevos (187 páginas).
- Commit `f5181b9`: "night(rank-history): UI de evolución de rango integrada en 01 · Ahora".

## Fase E — Encuentros PRO/STREAMER V1

- Reutilización total del Identity Registry existente (`config/known-players.ts`, `findKnownPlayerIdentity`) — el mismo matcher exacto-por-PUUID (con Riot ID como fallback) que ya usa "Partida en curso" desde el Sprint de Live Game. Ninguna lógica de matching nueva, ningún dato nuevo, ninguna re-verificación del dataset.
- `MatchParticipant.identity?: KnownPlayerIdentity` (nuevo campo, `types.ts`) — mismo tipo público que ya expone `LiveParticipant.identity`, sin inventar un segundo concepto de "identidad conocida".
- `normalizeMatch` (`normalize.ts`) acepta un resolutor opcional `identityFor?: (puuid) => KnownPlayerIdentity | undefined` (patrón curried, igual que `identityFor` en `live.ts`) — resuelve identidad por el PUUID que Riot YA trae en el DTO de cada participante (dato que antes se descartaba en la normalización). **+0 llamadas Riot**: opera sobre partidas ya obtenidas/cacheadas por `/api/riot/overview`. Excluido explícitamente para uno mismo (nunca "te encuentras contigo mismo"). Multi-cuenta: resuelto gratis por el propio `findKnownPlayerIdentity` (un `KnownPlayerIdentity` puede listar varios `puuids`).
- Wiring en `src/lib/riot/index.ts`: `getRiotOverview` pasa `(puuid) => findKnownPlayerIdentity(puuid, undefined, knownPlayerIdentities)` a cada `normalizeMatch`.
- UI: badge "PRO"/"STREAMER"/"PRO / STREAMER" en el roster de Match History ya existente (`MatchExpanded`, nivel 2 de profundidad) — nunca una página `/pros` nueva, nunca una sección aparte. Tooltip (`title`) con nombre/equipo/rol reales. Sin badge no hay ninguna diferencia visual (0 falsos positivos posibles).
- Bug de especificidad CSS detectado y corregido en la propia sesión: `.match-teams li span` (más específico que una sola clase) pisaba el color del badge — resuelto calificando el selector (`.match-teams li span.encounter-badge`).
- QA: verificado en navegador inyectando una respuesta sintética de `/api/riot/overview` (sin tocar Riot real ni el Identity Registry real) con un oponente conocido PRO+STREAMER — el badge aparece exactamente en esa fila (`badgeCount: 1`), nunca en la propia o en un oponente desconocido; color/tamaño/posición confirmados vía estilos computados y `getBoundingClientRect` (sin overflow horizontal). Captura de pantalla del panel salió en blanco por el problema ya documentado de foco del panel de navegador — verificación completada por inspección DOM/CSS en su lugar.
- 5 tests nuevos (`test/lib/riot/normalize.test.ts`, sin cobertura previa de `normalizeMatch`): identidad real por PUUID exacto, exclusión de uno mismo, sin invención para un PUUID desconocido, comportamiento por defecto sin resolutor, colapso multi-cuenta a la misma identidad.
- Validación: 850/850 tests en verde, `npx tsc --noEmit` limpio, `npx eslint .` limpio, `npm run build` sin errores ni warnings nuevos (187 páginas).
- Commit `b086ca3`: "night(encounters): Encuentros PRO/STREAMER V1 en Match History".

## Fase F — Hardening

**Overflow horizontal (~16px, investigación heredada del encargo):**
- Reproducido y diagnosticado con precisión: es realmente DOS causas distintas.
  1. **Real, dentro de /competitivo, corregido**: `.live-match-list` es un `display:grid`; su hijo `.match-row-item` no tenía `min-width:0`, así que por defecto de CSS Grid (`min-width:auto`) el item nunca encogía por debajo del ancho mínimo de su contenido — a viewports muy estrechos (probado a 320px con partidas reales) esto forzaba hasta 60px de scroll horizontal en TODA la página, aunque `.match-row` ya tenía `flex-wrap` preparado para envolver y nunca llegaba a activarse. Corregido con una sola línea (`min-width: 0` en `.match-row-item`, `global.css`) — confirmado en navegador: de 60px a 0px de overflow real achacable a Match History.
  2. **Global, fuera de alcance, NO tocado**: el resto del overflow residual (4px a 375px, hasta ~20px a 320px) viene de `Environment.astro` (capa decorativa de fondo con sangrado intencional `-16px`/`+16px`), montado por `BaseLayout.astro` en TODAS las páginas del sitio — confirmado con `git diff c6c29d9..HEAD` que ningún archivo de este componente se ha tocado esta sesión. Es casi seguro el origen real del "~16px" que menciona el encargo original. Al ser un componente global (Home, Tier List, Campeones... todo lo usa), queda fuera del permiso de esta fase ("solo si está acotado a /competitivo") — se reporta, no se toca.
  3. También detectado (y descartado como objetivo): el `<header>`/Navbar global desborda ~49px a 320px exactos (un viewport muy estrecho, no uno de los tres oficiales de QA 375/820/1440) — mismo motivo, global, NO-TOCAR.

**Hardening de coste Riot:**
- `functions/api/riot/overview.ts`: `recordRankSnapshotIfPossible` resolvía el PUUID (`resolveSelfAccountPuuid`) incluso sin `env.DB` configurado (el estado real de producción hoy) — inofensivo en coste Riot real (la resolución ya iba a caché, nunca una llamada nueva) pero trabajo de más innecesario en cada petición. Corregido: `if (!env.DB) return;` antes de tocar nada.

**Tabla de coste Riot (visitante normal, isolate frío):**
| Llamada | Endpoint Riot | Caché (fresh / stale-hasta) | Disparada por |
|---|---|---|---|
| Account-V1 | `/riot/account/v1/accounts/by-riot-id/...` | 24h / 24h | `getRiotOverview` (primera de la cadena) |
| Summoner-V4 | `/lol/summoner/v4/summoners/by-puuid/...` | 6h / 12h | `getRiotOverview` |
| League-V4 | `/lol/league/v4/entries/by-puuid/...` | 10min / 6h | `getRiotOverview` |
| Match-V5 ids | `/lol/match/v5/matches/by-puuid/.../ids` | 5min / 1h | `getRiotOverview` |
| Match-V5 partida | `/lol/match/v5/matches/{id}` (×≤10) | 24h / 7d cada una | `getRiotOverview` |
| Data Dragon versión | (CDN, no cuenta contra el rate limit de la API-KEY) | — | `getRiotOverview` |

Ninguna fila nueva desde el baseline (`c6c29d9`). Lo que añadió Night Shift:
- `/api/riot/rank-history` (Fase D): 0 llamadas Riot propias — lee D1. Solo si el PUUID de sesión aún no está cacheado, dispara como mucho la MISMA Account-V1 de la tabla de arriba (nunca una nueva categoría de llamada).
- Encuentros PRO/STREAMER (Fase E): 0 llamadas Riot — cálculo puro sobre partidas ya traídas por `getRiotOverview`.
- `/api/riot/rank-snapshot-cron` (Fase C): 0 llamadas en tráfico normal de visitantes (nadie lo dispara solo) — cuando SÍ se invoca externamente con el secreto correcto, cuesta exactamente la tabla de arriba una vez, ni más ni menos (reutiliza `getRiotOverview` íntegro, nunca una llamada Riot propia).
- Conclusión: **el coste Riot por visita normal a /competitivo es idéntico al de antes de esta sesión.**

**Accesibilidad (elementos nuevos de esta sesión):**
- `RankEvolution`: SVG con `role="img"` + `aria-label` descriptivo; sin elementos interactivos (nada que gestionar en foco/tabindex).
- `.encounter-badge`: contraste medido `#d6b56b` sobre fondo real ≈9.5:1 (supera AAA); texto visible como parte del flujo normal (nunca solo en `title`, que no es fiable para lectores de pantalla) — "PRO"/"STREAMER" se lee igual que el resto de la fila.
- `.rank-evolution-transition` (`#9fb3d6`): contraste medido ≈9.3:1.
- Ningún elemento nuevo añade paradas de teclado fantasma (badges y meta-texto son `<span>`/`<p>` sin `tabindex`).

**Matriz responsive (375/820/1440, más 320 como caso límite):** verificada en navegador para los 4 estados de `RankEvolution` (hidden/empty/single/chart) y el badge de Encuentros — sin problemas de legibilidad ni de layout en ninguno, salvo el overflow ya descrito y corregido en Match History.

**Limpieza de código muerto (acotada a esta sesión):** revisado — ningún export nuevo de Fases B-F quedó sin usar (`riotRankHistory`, todos los módulos de `rank-history`, `RankEvolution.astro`, campos nuevos de `MatchParticipant`); nada que limpiar.

**Rendimiento:** `RankEvolution.astro` compila a ~3KB de JS minificado (script propio, sin librerías nuevas); `npm run build` sigue en ~1.3s / 187 páginas, sin cambios apreciables de tamaño de página (`dist/competitivo` ≈32KB de HTML estático, el resto en bundles `_astro` ya compartidos).

- Validación: 850/850 tests en verde, `npx tsc --noEmit` limpio, `npx eslint .` limpio, `npm run build` sin errores ni warnings nuevos (187 páginas).

---

## Cierre — D1 + automatización de snapshots + pre-merge (2026-09-10)

Sesión de cierre: dejar la feature OPERATIVA o preparada al 100% con
instrucciones exactas. Sin funcionalidades nuevas.

**Auditoría del código Night Shift (verificado, no confiando en el informe previo):**
- Dominio (`types`/`rank-order`/`dedupe`/`evolution`/`observe`) correcto: timestamps ISO 8601 UTC (orden lexicográfico == cronológico), Master+ con `rank` nullable manejado en todo el orden, dedupe por estado + heartbeat 6h, nunca backfill.
- **BUG CRÍTICO encontrado (§18):** `rank-snapshot-cron.ts` llamaba a `getRiotOverview()` completo — arrastra SUMMONER-V4 + MATCH-V5 ids + hasta 30 detalles de partida + Data Dragon. Ejecutar eso cada 30 min solo para leer LP es exactamente lo que el encargo prohíbe.
- Idempotencia (§16): `recordObservationIfDue` hacía read→decide→insert sin backstop; dos invocaciones simultáneas podían duplicar.

**Correcciones:**
- Nuevo camino ligero `getRiotRankObservation()` en `src/lib/riot/index.ts`: SOLO ACCOUNT-V1 (caché 24h, clave compartida `riot:account:*`) + LEAGUE-V4 (caché 10min, clave compartida `riot:ranked:{puuid}`). Nunca SUMMONER-V4 / MATCH-V5 / Timeline / Data Dragon. `rank-snapshot-cron.ts` reescrito para usarlo.
- `migrations/0001_rank_snapshots.sql`: base renombrada a `tidusss-competitive` (§4), añadida `UNIQUE (puuid, queue_type, observed_at)`, eliminado el índice explícito redundante (la UNIQUE ya crea su índice y cubre las 5 consultas). Migración idempotente para base nueva.
- `d1-repository.ts` `insert`: `INSERT OR IGNORE` + devuelve `null` en colisión (`changes: 0`). Interfaz `RankSnapshotRepository.insert` → `Promise<RankSnapshot | null>`. `observe.ts` mapea `null` → `{ inserted: false, reason: 'duplicate' }` (nuevo motivo, nunca un error).
- Respuesta del cron enriquecida (§28): `observed` (tier/rank/LP/wins/losses/stale), `recorded`, `reason`, `observedAt` — nunca PUUID ni secreto.
- `overview.ts` (escritura oportunista): sin cambios de lógica, sigue reutilizando datos ya traídos por `getRiotOverview` (0 coste Riot añadido). Se mantienen AMBAS vías (oportunista + cron) porque comparten la misma deduplicación + la UNIQUE de backstop (§17).

**Aprovisionamiento D1:** `wrangler` NO está instalado ni autenticado en este entorno, y no hay token de Cloudflare. No se crea ningún recurso remoto. Todo lo necesario queda preparado; los pasos humanos exactos están en `docs/operations/rank-history.md`.

**Validación local del SQL real (§9):** nuevo `test/lib/rank-history/d1-repository.sqlite.test.ts` — corre `D1RankSnapshotRepository` contra un SQLite real (`node:sqlite`, módulo integrado, sin dependencia nueva) con el schema real de la migración aplicado. Valida: parseo del SQL en SQLite, `AUTOINCREMENT`/`last_row_id`, orden cronológico sobre `observed_at TEXT`, `getPeak` por rango real, Master+ con `rank` NULL, y la UNIQUE + `INSERT OR IGNORE` (colisión → `changes:0` → `null`, sin fila duplicada).

**Scheduler (§12/§13) — decisión:** GitHub Actions programado (`*/30`) → `POST /api/riot/rank-snapshot-cron`. El repo ya vive en GitHub, cero recursos nuevos en Cloudflare, cero `wrangler.toml`, versionado, apagable con un clic. El jitter de GHA es irrelevante (heartbeat 6h + observación oportunista en cada visita). `.github/workflows/rank-snapshot.yml` añadido; los `schedule` solo corren desde `main`, así que no dispara nada en la rama. Alternativa (Worker + Cron Trigger) documentada en el runbook para quien quiera timing preciso.

**Nuevos archivos:** `docs/operations/rank-history.md`, `.github/workflows/rank-snapshot.yml`, `test/lib/rank-history/d1-repository.sqlite.test.ts`, `test/lib/riot/rank-observation.test.ts`. **Modificados:** `src/lib/riot/index.ts`, `functions/api/riot/rank-snapshot-cron.ts`, `migrations/0001_rank_snapshots.sql`, `src/lib/rank-history/{types,d1-repository,observe}.ts`, `test/lib/rank-history/{d1-repository,observe}.test.ts`, `test/api/rank-snapshot-cron.test.ts`, `.env.example`.

**Validación de cierre:** 867/867 tests en verde (+17), `npx astro check` limpio, `npx tsc --noEmit` limpio, `npx eslint .` limpio, `npm run build` sin errores ni warnings nuevos (187 páginas).
