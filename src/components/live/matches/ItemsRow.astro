<div class="match-assets match-items">
  <span>Objetos</span>
  <div data-match-items>
    {
      Array.from({ length: 7 }, (_, index) => (
        <i data-item-slot={index}>
          <img width="32" height="32" alt="" loading="lazy" decoding="async" />
        </i>
      ))
    }
  </div>
</div>
